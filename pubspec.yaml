name: dmhelper
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: 
  font_awesome_flutter: 
  flutter_svg: 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  fonts:
    - family: Space
      fonts:
        - asset: lib\assets\Space_Grotesk_Font\SpaceGrotesk-Regular.ttf

  assets:
    - lib\assets\amorClassIcon.svg
    - lib\assets\campaignsIcon.svg
    # Add all your custom SVG icons here
